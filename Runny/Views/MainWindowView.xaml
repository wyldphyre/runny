<Window x:Class="Runny.ViewModels.MainWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Runny"
        xmlns:viewModels="clr-namespace:Runny.ViewModels"
        mc:Ignorable="d"
        Title="Runny" Height="300" Width="300"
        d:DataContext="{d:DesignInstance IsDesignTimeCreatable=True, Type={x:Type viewModels:MainWindowViewModel}}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Border CornerRadius="4" Margin="4" BorderThickness="1" BorderBrush="LightGray" Background="Azure">
            <Label Content="{Binding Title}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="14" />
        </Border>
    </Grid>
</Window>
